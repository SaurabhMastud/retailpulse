# Copy to .env and fill in for local runs. Never commit the real .env.

# Path to the DuckDB warehouse file used by ingestion, dbt, and the dashboard.
RETAILPULSE_WAREHOUSE_PATH=data/warehouse/retailpulse.duckdb

# Airflow (set once the DAG lands on day 5)
AIRFLOW_HOME=./.airflow
